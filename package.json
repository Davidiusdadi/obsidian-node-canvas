{
  "name": "workspace",
  "version": "1.0.0",
  "private": "true",
  "scripts": {
    "dev": "env $(cat .env) pnpm --filter canvas-engine run dev",
    "dev:inspect": "env $(cat .env) pnpm --filter canvas-engine run dev:inspect",
    "dev:inspect-brk": "env $(cat .env) pnpm --filter canvas-engine run dev:inspect-brk"
  }
}